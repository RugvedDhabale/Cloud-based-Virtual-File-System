services:
  - type: web
    name: syscall-visualizer-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api.server:app --host 0.0.0.0 --port $PORT"
